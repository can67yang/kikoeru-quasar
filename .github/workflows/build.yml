name: Build frontend

on:
  workflow_dispatch:

jobs:
  build:
    name: Build PWA
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build (PWA, no sourcemap)
        run: npx quasar build -m pwa

      - name: Upload dist artifact
        uses: actions/upload-artifact@v4
        with:
          name: kikoeru-quasar-dist
          path: dist/pwa
          if-no-files-found: error
